type Task @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String!
  description: String
  dateCreated: AWSDateTime
  taskStatusEnum: TaskStatusEnum

}

enum TaskStatusEnum {
  New
  Assigned
  In_Progress
  Complete
}